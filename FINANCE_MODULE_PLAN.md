# Module Finances — Plan & Évolution

Branche : `feature/finance-module` (depuis `develop`)
Dernière mise à jour : 2026-06-27 — Lot 2 terminé

## Objectif
Refonte du système de frais, tranches de paiement et inscription annuelle pour EduApp.
Trois problèmes identifiés :
1. **Frais rigides** — frais liés à la classe globale, pas modulaires par élève (genre, cantine, transport, remises impossibles).
2. **Aucune dimension temporelle** — paiement = simple compteur, sans dates d'exigibilité ni tranches réelles.
3. **Pas de colonne vertébrale annuelle** — `Student.school_class` est l'unique vérité « présent éternel » ; `StudentEnrollment` inerte ; pas de réinscription.

## Principes directeurs
- Migrations **additives uniquement** (champs nullable ou défauts, jamais de rupture).
- `Payment` reste un **journal de transactions immuable** — on n'y touche pas ; l'allocation se fait via une table dédiée.
- Le solde se **calcule**, ne se stocke jamais en champ muté.
- Frais accrochés à l'**année scolaire** (via `StudentEnrollment`), pas au `Student` directement.
- Modèle « défaut + exception » : config école héritée par toutes les classes, surchargeable au cas par cas.

## Architecture cible (modèle de données)
- **Catalogue de frais** (niveau école) : type, montant, obligatoire/optionnel, périodicité, variantes.
- **Variantes de frais** : tenue (fille/garçon, auto par genre), bus (par trajet), cantine (par formule).
- **Fiche financière élève** : postes dus par élève, accrochés à l'enrollment de l'année.
- **Échéancier** : lignes datées (montant attendu, date limite, statut), générées par un gabarit.
- **Allocation de paiement** : table liant `Payment` ↔ échéance(s), avec montant ventilé.
- **Gabarits de tranches** (niveau école) : annuel / trimestriel / mensuel, surchargeable par élève.

## Lots d'exécution
- [x] **Lot 1 — Socle temporel** : réveiller `StudentEnrollment` (source de vérité, contrainte unique `(élève, année)`, statut `ACTIVE` écrit) + champ genre sur `Student`. Modèles + migration, sans UI.
- [x] **Lot 2 — Catalogue de frais** : modèles catalogue + variantes + écran de config école.
- [x] **Lot 3 — Fiche financière par année** : postes élève + échéancier + allocation, liés à l'enrollment actif.
- [x] **Lot 4 — Inscription enrichie** : flux générant la fiche financière à l'inscription. _(4a : fondation enrollment + inscription unitaire enrichie. 4b : import de masse enrichi — colonne genre + fiches en bulk. Flux Groupe supprimé entre-temps.)_
- [x] **Lot 5 — Encaissement + timeline** : écran de paiement au guichet (timeline de tranches colorées).
- [x] **Lot 6 — Tableau de bord impayés + REFONTE de la page Paiements** : liste rouge filtrée par date d'échéance (nouveau modèle : tranches datées, 3 familles, allocations). Page `payment_dashboard` basculée sur le nouveau modèle ; encaissement unifié sur `collect_create` ; ancien `payment_create` non-alloué **supprimé**.
- [x] **Lot 6bis-A — Helper financier central + 5 écrans migrés** : helper unique dans [apps/finance/services.py](apps/finance/services.py) (`student_fee_summary` par objet → None si pas de fiche ; `annotate_students_with_fees` par qs → `fee_status` ∈ paid/partial/unpaid/**no_fee** ; `fee_accounts_annotated` agrégats due/paid/balance ; `count_without_account`). Migrés : **badge statut** (4ᵉ état neutre, jamais rouge si pas de fiche, partial `fee_status_badge.html` partagé) ; **liste élèves** (filtres = fiche, sans-fiche exclus de Impayés/Partiel/Soldés mais visibles en « Tous », stat « X sans fiche ») ; **dashboard directeur** (unpaid_count/objectif/taux santé classes, « pas de données » si 0 fiche, KPI cash inchangés) ; **reçu PDF** (solde via helper, repli « non disponible ») ; **promoteur** (dû ET versé sur la même population → taux honnête). Page Paiements (lot 6) pointe désormais sur `fee_accounts_annotated` (source unique). Test e2e 13/13. `tuition_fee`/`get_*` conservés (parent + reculs).
- [x] **Lot 6bis-B — Portail parent** : `parent_dashboard` + `parent_payments` ([apps/parent/views.py](apps/parent/views.py)) → timeline 3-familles de l'enfant via le helper central (`student_fee_summary` + `timeline_families`), en **lecture seule**. `timeline.html` rendu réutilisable par un flag `readonly` (masque le bouton Encaisser / collect-panel — admin inchangé). Solde parent **identique** à la fiche admin et à la page Paiements. État neutre « Fiche à venir » si pas de fiche (jamais de solde faux). Sécurité : chaque enfant via `request.user.guarded_students` uniquement, `?child=` sélectionne dans la liste déjà filtrée (aucun accès par `student_id`). Test 21/21 (timeline lecture seule, isolation enfant non lié, sans-fiche neutre, cohérence solde, admin non régressé, écrans non-financiers intacts).
- ✅ **Unification financière COMPLÈTE** : plus AUCUN écran sur l'ancien modèle (`tuition_fee - paid`). Tous les soldes proviennent du helper central (`StudentFeeAccount` + allocations). `tuition_fee`/`get_*` conservés comme champ/repli legacy uniquement (plus aucun écran ne les affiche).
  _(Hors périmètre : superadmin & totaux de cash réel `Sum(Payment)` = trésorerie, corrects ; compta/paie `SalaryPayment` = modèle distinct ; admin Django optionnel.)_
- [ ] **Lot 7 — Passage de classe** : flux de fin d'année (régénération fiche année N+1) — boucle et valide l'architecture annuelle.

## Décisions actées
- Bus/cantine = **abonnements** mensuels résiliables ou forfait annuel, **pas** des tranches de scolarité.
- Frais à **variantes** (un concept unique couvre tenue, bus, cantine).
- Scolarité reste **par classe** (`SchoolClass.annual_fee`), découpée par gabarit.
- Inscription = frais ponctuel annuel, règle « nouveaux / anciens » configurable au niveau école.
- Passage de classe construit **maintenant** (lot 7) pour valider l'architecture, pas reporté.
- **Refonte de la page Paiements au lot 6** : `payment_dashboard` (filtres Impayés/Partiel/Soldés + stats) et son panneau d'encaissement (`payment_create`) reposent encore sur l'ANCIEN modèle (solde global `tuition_fee`/`get_total_paid`, sans tranches ni allocation). Le lot 6 les bascule sur le nouveau modèle (3 familles, tranches datées, allocations) et fait pointer l'encaissement vers `collect_create`. L'ancien `payment_create` non-alloué sera redirigé ou retiré.
- **Frais nouveaux/anciens (lot 3)** : champ `applies_to` sur `FeeType` (NEW / RETURNING / ALL) + `returning_amount` nullable. Configurable ET automatique : le socle du lot 1 sait reconnaître un ancien élève via un `StudentEnrollment` de l'année précédente (présence d'un enrollment antérieur dans la même école). NEW = uniquement nouveaux inscrits, RETURNING = uniquement anciens (avec éventuel `returning_amount` réduit), ALL = tous.
- **Fiche financière = TROIS familles de dettes indépendantes**, jamais fondues : (a) scolarité en tranches datées, (b) frais ponctuels one-shot, (c) abonnements mensuels résiliables. L'allocation d'un paiement se fait **à l'intérieur de chaque famille**, jamais dans un sac FIFO commun — un versement scolarité ne solde pas une dette cantine, et inversement.
- **Dates des tranches** : portées par le gabarit `PaymentScheduleTemplate`, **pré-remplies depuis les `Period` existants** de l'année active (cf. `schools.Period`), puis ajustables à la génération de l'échéancier.

## Évolutions futures (hors périmètre actuel)
- **Montant de frais variable par niveau/classe** (ex. inscription Jardin ≠ inscription 9ème). V1 : montant unique par frais ; une école qui en a besoin crée des frais distincts (ex. « Inscription primaire », « Inscription collège »). À implémenter plus tard via une petite table « montant par niveau » accrochée au `FeeType`, si la demande émerge.
- **Nettoyage `components.css` orphelin** (`brand-blue` hors build) — à supprimer ou réaligner sur `primary`. Hors périmètre finances.
- **Import 1000 élèves synchrone** — à valider en charge ; passer en asynchrone si nécessaire. Dépend de l'infra de tâches de fond (Celery/worker), **non décidée** à ce jour (aujourd'hui : aucune, hors thread daemon ad hoc des leçons).

## Points de vigilance (relevés aux audits)
- Incohérence charte : `brand-blue` (components.css) vs `primary-600/700` (templates) — trancher la couleur officielle.
- ~~Bug latent fiche élève : `payment.is_valid` / `payment.paid_at`~~ — corrigé au lot 3, bloc remplacé par la timeline au lot 5.
- Import 1000 élèves synchrone + sérialisation DOM — à valider en charge.
- `School.current_school_year` (CharField décoratif) coexiste avec `SchoolYear.is_active` (structurant) — ne pas confondre.
- `StudentEnrollment` sans contrainte d'unicité aujourd'hui — à ajouter au lot 1.
- `components.css` orphelin (`brand-blue` hors build) — à supprimer ou réaligner sur `primary`, hors périmètre finances.
- ~~**Deux modèles de solde coexistent**~~ → **RÉSOLU (lot 6bis complet)** : tous les écrans (page Paiements, modale, badge, liste élèves, dashboard, promoteur, reçu, **portail parent**) consomment désormais le helper central (`StudentFeeAccount` + allocations). Plus aucune divergence possible.
- **`Student.get_total_paid()` / `get_balance_due()` / `get_payment_status()` / `tuition_fee`** : ne sont **plus affichés par aucun écran**. Conservés uniquement comme champ/repli legacy (ex. snapshot d'inscription, `build_fee_account` lit `tuition_fee` pour le montant scolarité). Suppression possible plus tard après audit des derniers usages internes — sans impact UI.

## Journal des sessions
| Date | Lot | Ce qui a été fait |
|------|-----|-------------------|
| 2026-06-28 | 6bis-B | **Portail parent : timeline financière lecture seule → unification COMPLÈTE.** Dernier écran « ancien modèle » migré. **(1) `timeline.html` réutilisable** : bouton Encaisser (+ dépendances `collectOpen`/`#collect-panel-body`/`finance:collect-panel`) encadré par `{% if account and not readonly %}` → admin inchangé (readonly absent=falsy), parent en lecture seule. timeline.html ne porte aucun `id` (conteneur externe) → pas d'id dupliqué côté parent. **(2) `parent_dashboard`** : calcul `tuition_fee - paid` remplacé par `student_fee_summary(child)` (None→état neutre « Fiche à venir », jamais de solde faux) ; timeline détaillée de l'enfant ACTIF (préfetch `debts→installments→allocations`, zéro N+1) via `timeline_families`, incluse avec `readonly=True`. Mono-enfant à la fois (`?child=`) → une seule timeline/page. **(3) `parent_payments`** : solde via le helper (= fiche admin) ; journal des Payment bruts conservé (immuable) ; cas no_fee neutre. **SÉCURITÉ** : chaque enfant via `request.user.guarded_students` uniquement, `?child=` filtre dans la liste déjà gardée — aucun accès par `student_id`. Test 21/21 : timeline lecture seule (3 tuiles, pas d'Encaisser/collect-panel), **isolation** (enfant non lié jamais affiché, `?child=<non lié>` ignoré), sans-fiche neutre sans plantage, **solde parent == solde admin (page Paiements)**, **admin non régressé** (Encaisser + collect-panel présents), écrans non-financiers parent (bulletins/notes/annonces) intacts. `check` 0 issue, `makemigrations --check` propre, base #4 intacte (test en rollback). **Plus aucun écran sur l'ancien modèle.** |
| 2026-06-27 | — | Création branche + plan initial |
| 2026-06-27 | 3 | Cœur du module. FeeType amendé (`applies_to` NEW/RETURNING/ALL + `returning_amount`, helpers `applies_to_student`/`resolved_amount`). 4 modèles : `StudentFeeAccount` (1/enrollment, agrégats calculés), `FeeDebt` (3 familles via `kind`, snapshot `total_amount`, résiliable), `Installment` (échéance datée, `is_overdue`/`days_overdue`), `PaymentAllocation` (lien Payment↔tranche — immuabilité préservée, aucun solde stocké). Migration `0004`. Services : `build_fee_account` (scolarité+frais obligatoires applicables, variante auto par genre, idempotent), `generate_tuition_installments` (découpe sans perte de franc, dates dérivées des Period sinon segments d'année), `allocate_payment` (FIFO intra-dette, anti sur-allocation, jamais inter-familles). Commande `build_fee_account_for_student`. Bloc fiche élève réparé (`is_valid`→`not is_cancelled`, `paid_at`→`payment_date`) + affichage minimal de la fiche (3 familles, tranches, statuts) avec état neutre si absente. Test e2e 32/32 (NEW vs RETURNING, 100000/3 exact, dates=périodes, allocation partielle/cascade/anti-surallocation/inter-famille, idempotence, vue réparée). Aucun champ Payment ajouté. Base dev intacte (test en rollback). |
| 2026-06-28 | 6bis-A | **Helper financier central + 5 écrans migrés.** Audit du cas « élève sans fiche » (le nouveau modèle suppose une `StudentFeeAccount`, absente pour les élèves non encore entrés par l'inscription). Helper UNIQUE dans `finance/services.py` : `student_fee_summary(student)` → None si pas de fiche (état neutre obligatoire, jamais rouge) sinon {due,paid,balance,status,has_overdue} ; `annotate_students_with_fees(qs)` → fee_status ∈ paid/partial/unpaid/**no_fee** (1 requête, pas de N+1) ; `fee_accounts_annotated(school/school_ids)` agrégats due/paid/balance des fiches ; `count_without_account`. Écrans migrés : **(A) badge statut** (`fee_status_badge.html` partagé, 4ᵉ état neutre « — », row/card/profile) ; **(B) liste élèves** (`_students_qs`/`compute_student_stats` : sans-fiche exclus des 3 filtres mais visibles en « Tous » + stat « X sans fiche ») ; **(C) dashboard directeur** (`unpaid_count`=balance>0, objectif=Σ due, taux santé classes sur même population, « pas de données »/`None`→« — » si 0 fiche ; KPI cash `Sum(Payment)` INCHANGÉS) ; **(D) reçu PDF** (solde via helper, repli « non disponible » + statut « Indisponible » si legacy sans fiche ; détail d'allocation intact) ; **(E) promoteur** (synthèse/écoles/détail : dû ET versé depuis les fiches → taux honnête, plus de gonflage). Page Paiements (lot 6) re-pointée sur `fee_accounts_annotated` (source unique, plus de logique inline). Test e2e 13/13 (sans-fiche neutre partout, chiffres cohérents liste/fiche/Paiements, promoteur + dashboard + reçu rendent). `check` 0 issue. **6bis-B (portail parent) reste à faire.** `tuition_fee`/`get_total_paid`/`get_balance_due`/`get_payment_status` CONSERVÉS (parent + reculs). |
| 2026-06-27 | 6 (finition) | Après audit des 8 zones financières « ancien modèle », correction des 2 plus urgentes (incohérence directe avec la page refondue) : **(1) Modale historique** (`payment_timeline.html` + `payment_history`) — tuiles Total dû/Versé/Solde basculées sur `StudentFeeAccount.total_due/paid/balance` (3 familles par allocation), repli neutre si pas de fiche ; le journal des Payment réels conservé. **(2) Badge alerte global** (`alert_count`, context_processor) — passé à « nb d'élèves avec ≥1 tranche échue impayée » (= définition de la liste rouge du lot 6), 1 requête (distinct count, due_date indexée), élèves sans fiche ignorés. Les 6 autres zones (parent, liste élèves, dashboard, promoteur, reçu) **actées en Lot 6bis** — `tuition_fee`/`get_*` conservés jusque-là. Test 5/5 (modale=fiche 3 familles, alert_count==liste rouge, cas sans fiche). |
| 2026-06-27 | 6 | **Tableau de bord impayés + unification de la page Paiements.** `payment_dashboard` basculé sur le NOUVEAU modèle : stats (dû/versé/solde/soldés/impayés) = agrégats des `StudentFeeAccount` par allocation (sous-requêtes `due_sq`/`paid_sq`, pas de Sum multi-jointures) ; « encaissé ce mois » = trésorerie réelle (Payment du mois) ; élèves actifs sans fiche signalés à part (`sans_fiche`, 0 en prod). Tableau/cartes basés sur les fiches (`account.due/paid/balance`, statut par allocation), « + Versement » ouvre le panneau d'allocation du lot 5 (`finance:collect-panel`). **Onglet « En retard » = liste rouge** : élèves avec ≥1 `Installment.due_date < today` et solde > 0 → montant en retard, nb de tranches, jours d'ancienneté (plus vieille échéance), tri par gravité (jours desc, montant desc), filtre classe/recherche, action « Encaisser » + bouton « Relancer » désactivé (placeholder évolution SMS). **Ancien flux supprimé** : `payment_create` + `payment_form` (vue, template, partial `payment_create_success`, route `payments:form`/`create`, `PaymentCreateForm`) retirés ; conservés : reçu PDF, `payment_cancel`, historique, receipt_*. Panneau d'encaissement rendu host-agnostic (fermeture par `$dispatch('close-collect-panel')`) → réutilisé sur fiche élève ET dashboard ; `collect_create` émet `payment-collected` → le dashboard rafraîchit liste+stats. Un encaissement depuis la page Paiements crée bien des allocations (vérifié). Test e2e 14/14, base #4 propre. **Un seul système de paiement dans toute l'app.** Décision actée : pas d'allocation rétroactive des anciens paiements (base prod vierge). |
| 2026-06-27 | 5 (correction double bloc) | Retrait de l'ANCIEN bloc « Situation financière » de la fiche élève (3 tuiles `tuition_fee`/`get_total_paid`/`get_balance_due` + historique) qui coexistait avec la timeline du lot 5 et affichait un « Versé » contradictoire. La **timeline est désormais le seul résumé financier** (dû/versé/reste = agrégats par allocation). Un **« Journal des versements »** unique est conservé sous la timeline, alimenté par les `Payment` réels (journal immuable), avec un marqueur « non affecté » sur les paiements sans allocation (anciens / page Paiements). Cadrage **lot 6** acté : refonte/unification de `payment_dashboard` + bascule de son encaissement vers `collect_create` (cf. Décisions actées + Points de vigilance). Autres sections de la fiche intactes. |
| 2026-06-27 | 5 | **Encaissement au guichet + timeline.** Allocation câblée à l'écran réel : depuis la fiche élève, bouton « Encaisser » → panneau slide. Back-end (`apps/finance/views.py` + `urls.py` montées sur `/finance/`) : `collect_panel` (familles dues via `payable_targets`, plus en retard pré-sélectionnée), `collect_preview` (aperçu FIFO live via `allocation_plan`, sans rien enregistrer), `collect_create` (Payment immuable → `allocate_payment` FIFO intra-famille → reçu + notif parent). **Décision pay-as-you-go abonnements** : `generate_subscription_installments` crée la mensualité du mois À l'encaissement (idempotent par mois ; option « année entière » = N mois jusqu'à fin d'année). **Jamais de sur-allocation** : montant > solde famille → refusé (422 + message), pas de surplus orphelin. Reçu PDF standard enrichi du détail d'allocation (dette — tranche → montant). Timeline de consultation (`finance/partials/timeline.html`) remplace le bloc minimal du lot 3 : 3 tuiles calculées + scolarité en tranches colorées (vert/orange/rouge/gris, **empilées sur mobile**) + ponctuels + abonnements pay-as-you-go ; ré-rendue en OOB après encaissement (`timeline_families` source unique). Helper `_get_debt` borne l'accès à la fiche de l'élève (sécurité). Test e2e 15/15 (partiel, cascade, jamais inter-familles, trop-perçu refusé, pay-as-you-go + idempotence + année, aperçu = réalité, reçu, panneau). JSON single-encodé, JS Alpine validé, 0 commentaire fuité. Non-régression dashboard paiements / liste / fiche sans compte. Base #4 propre (tests en rollback). Paiements existants non alloués = hors périmètre (séparé). |
| 2026-06-27 | 4b | **Import de masse enrichi + test de charge.** Colonne « Genre (G/F) » alignée aux 4 endroits : template `.xlsx` (en-tête + exemples G/F), parsing (`gender_map` tolérant : F/Fille/Féminin/Femme→F, G/M/Garçon/Masculin/Homme→M ; vide/illisible→None **jamais bloquant**), aperçu (colonne Genre + « non renseigné »), confirm (`gender` dans `Student()`). Fiche à l'import : scolarité (gabarit défaut) + frais obligatoires + **tenue auto par genre** ; **aucune option facultative** (bus/cantine se cochent en individuel — décision actée). Garde-fou année active → **422 + message** avant toute création (ne crée pas 1000 élèves sans fiche). Perf : `build_fee_accounts_bulk(enrollments)` (catalogue/gabarit/périodes préchargés 1×, enrollments + accounts + debts + installments en bulk_create) ; helpers `_split_amount`/`_due_dates_for_year`/`_template_count` extraits et partagés avec la version unitaire. **Test de charge 1000 élèves : 9,15 s → 0,50 s (×18), ~9000 requêtes → 12 (constant)**, sortie identique (1000 fiches / 3000 dettes / 5000 tranches). Synchrone optimisé largement suffisant (<10 s), pas besoin d'async. Test fonctionnel 23/23 (normalisation genre, tolérance, tenue auto F/G, pas d'abonnement à l'import, doublons toujours détectés). Non-régression individuel OK. Base #4 restaurée (244 élèves, Tenue re-optionnelle). **Lot 4 complet.** |
| 2026-06-27 | 4a (suppression flux Groupe) | Retrait du mode « Groupe » du panneau d'inscription (audit décisionnel : niche étroite — saisie de N noms sans fichier — déjà couverte par l'import, en mieux : multi-classes, aperçu, anti-doublon ; et données dégradées : genre null, fiche minimale, même pas les colonnes tél/parent/date). Le panneau n'a plus d'onglet, il s'ouvre directement en inscription **individuelle**. Supprimés : onglet/toggle + bloc form groupe (`student_list.html`), état Alpine (`panelMode`, `groupNames`, `groupClassId`, `addGroupName`, `removeGroupName`), vue `student_create_group`, route `create/group/`, et la dernière réf `panelMode` dans `student_table_body.html`. Reste deux flux clairs : **Individuel** (détaillé) + **Import** (masse). Vérifs : grep zéro référence morte, `students:create-group` introuvable (NoReverseMatch), `check` 0 issue, GET /students/ = 200 (#4 et #5, plus d'onglet Groupe), individuel + import toujours fonctionnels (smoke en rollback). |
| 2026-06-27 | 4a (fix robustesse) | Bug critique : **double encodage JSON** (`json.dumps` dans la vue + `\|json_script` dans le template) → `JSON.parse` rendait une chaîne, `this.fees.filter`/`schedules.find` plantaient, `init()` levait → **tout le composant Alpine mourait** (undefined, options disparues, récap « F » seul). Manifesté pire sur école non configurée mais présent partout (les e2e ne lançaient pas le JS). Corrigé : la vue passe des **objets Python**, `json_script` encode une seule fois. + Robustesse école « démarrage » (0 frais / 0 gabarit) : parsing défensif `safeParse`→[], `currentTemplateCount` repli 1, `currentTemplateName`=« Paiement en une fois », bandeau d'info ambre « pas de catalogue → Paramètres » avec lien, options affichent une raison si vides (jamais un trou), gabarit en état neutre, récap = au moins la scolarité (jamais vide). Validé sous Node sur école vide (#5) ET configurée (#4) : aucune exception, tenue auto 15000, récap correct. |
| 2026-06-27 | 4a | **Fondation enrollment + inscription unitaire enrichie.** Helper `apps/students/services.ensure_active_enrollment` (get_or_create de l'enrollment ACTIVE de l'année active, idempotent, None si pas d'année) branché dans les **3 flux** (unitaire/groupe/import) → plus aucun élève sans enrollment. `build_fee_account` étendu : `fee_selections` (options cochées) + `template` (gabarit choisi) ; abonnements cochés → dette subscription `is_active=True` **sans mensualité d'avance** (1ère au paiement, lot 5). Groupe/import = mode minimal (scolarité + obligatoires, genre nullable). `student_create` : garde-fou « pas d'année active » (422 + toast, pas de 500), lit genre/options/gabarit, crée enrollment + fiche ; `gender` ajouté requis au `StudentCreateForm`. Panneau enrichi (`student_list.html`) : genre segmenté Fille/Garçon obligatoire, bloc « Options et services » (toggles par frais optionnel, tenue auto par genre, select trajet bus), ligne gabarit repliable, **récap live** « à la rentrée » (1ère tranche + ponctuels, abonnements listés à part), bouton désactivé tant que genre/trajet manquants. Contexte JSON (`fees_json`/`schedule_json`) exposé au template. Test e2e 26/26 (Fille+tenue+bus+3 tranches, sans option, sans année active=422, idempotence, groupe). Base dev intacte (rollback). 4b (import enrichi) à venir. |
| 2026-06-27 | 3 (cadrage) | Décisions lot 3 actées : frais nouveaux/anciens (`applies_to` NEW/RETURNING/ALL + `returning_amount`, reconnaissance auto des anciens via `StudentEnrollment` N-1) ; fiche financière = 3 familles de dettes indépendantes (scolarité tranchée / ponctuels / abonnements), allocation intra-famille, jamais FIFO commun ; dates des tranches portées par le gabarit, pré-remplies depuis les `Period`. Section « Évolutions futures » ajoutée (montant par niveau, nettoyage components.css, import async). Audit lecture seule des écrans accueillant les lots 3/5/6. Aucun code modifié hors ce plan. |
| 2026-06-27 | 2 (corrections) | Corrections post-test visuel : (1) icône de carte décorative (`aria-hidden`, non cliquable) + différenciée par frais via `FeeType.get_icon()` (Lucide). (2) Variantes éditables inline (label+montant, auto-save HTMX) — fin du blocage « doublon Garçon » ; `gender_key` préservé. (3) Désactivation via le modal de confirmation maison (`$store.confirm`), suppression du `confirm()` natif. (4) « Supprimer » → **toggle Actif/Inactif** (frais ET variantes), section repliée « Frais désactivés » + réactivation ; contraintes d'unicité rendues **conditionnelles** (`is_active=True`) sur `(school,name)` et `(fee_type,label)` → un nom/libellé désactivé se libère, réactivation sans erreur (migration `0003`). (5) Scolarité sortie des cartes → **bannière d'info** (lien « Gérer les classes ») ; `FeeType` TUITION conservé en base (cohérence échéancier lot 3) mais exclu du catalogue ; non créable via le formulaire. `output.css` rebuildé (badges/utilitaires). Test e2e 18/18, base dev nettoyée. |
| 2026-06-27 | 2 | Nouvelle app `apps.finance`. Modèles `FeeType` (catalogue : catégorie one_time/tuition/subscription, montant nullable, obligatoire/variantes/genré), `FeeVariant` (label+montant+gender_key, unique par frais), `PaymentScheduleTemplate` (gabarit tranches, un seul défaut/école). Migrations `0001`+`0002`. Écran settings « Frais & tranches » (`/settings/frais/`) : catalogue en cartes (montant inline, badge « Par classe » scolarité, variantes), gabarits sélectionnables, état vide accueillant, HTMX+Alpine+toasts, classes UI réutilisées, primary-* only, responsive. Seed manuel (bouton dev + commande `seed_fee_catalog`). Test e2e 14/14 (CRUD frais/variantes, défaut unique, doublons refusés). Aucun champ Payment touché. |
| 2026-06-27 | 1 | Champ `Student.gender` (M/F, nullable) + `Gender` TextChoices. Contrainte unique conditionnelle `uniq_enrollment_student_year` sur `StudentEnrollment (student, school_year)` (condition `school_year__isnull=False`). Docstring « source de vérité » sur le modèle. Migration schéma `0006` + data migration `0007` (backfill enrollments ACTIVE des élèves actifs vers l'année active, idempotente, reverse no-op). Backfill vérifié : 1074/1074 élèves rattachés, 0 doublon, école sans année active ignorée. Aucune UI touchée. |
